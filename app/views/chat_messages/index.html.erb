<% content_for :title, "Chat messages" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Weather ChatBot</h1>
  </div>

<%
=begin%>
 <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-4 bg-gray-200 h-screen flex justify-center items-center"> 
<%
=end%>

<div class="w-full h-screen justify-center items-center">
  <div class="flex flex-col space-y-4 h-full">
    <div class="flex-1 overflow-y-auto p-4 space-y-3" data-controller="scroll">
      <%= turbo_stream_from :chat_messages %>

      <%= turbo_frame_tag :chat_messages do %>
        <%= render @chat_messages %>
      <% end %>
    </div>
    <%= render partial: 'form', locals: { chat_message: ChatMessage.new } %>
  </div>
</div>

<%
=begin%>
 </div> 
<%
=end%>



</div>
